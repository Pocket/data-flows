{
  "name": "data-flows",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "dist/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "build:dev": "rm -rf dist && NODE_ENV=development npm run synth",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok"
  },
  "engines": {
    "node": "=16"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "^2.2.14"
  },
  "devDependencies": {
    "@types/node": "^16.11.10",
    "typescript": "4.5.2"
  }
}
